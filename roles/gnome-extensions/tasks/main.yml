---
- name: gnome-extensions
  tags:
    - never
    - gnome
    - gnome-extensions
  block:
    - name: Install gnome-extensions (flatpak)
      community.general.flatpak:
        name: org.gnome.Extensions
        state: present
        remote: flathub

