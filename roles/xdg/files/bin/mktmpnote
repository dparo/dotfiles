#!/usr/bin/env bash

run() {
    nohup "$@" >/dev/null 2>&1 &
    disown
}

mkdir -p "/tmp/$USER/notes"

file="$(mktemp -p "/tmp/$USER/notes/")"-"$(date)".md

if builtin type marktext 1>/dev/null 2>/dev/null; then
    touch "$file"
    run marktext "$file"
else
    run kitty -- nvim "$file"
fi

