---
- name: Mermaid
  tags:
    - never
    - writing
    - mermaid
  block:
    - name: Install mermaid-cli
      community.general.npm:
        name: "{{ item }}"
        global: true
        state: present
        version: 11.14.0
      loop:
        - "@mermaid-js/mermaid-cli"
