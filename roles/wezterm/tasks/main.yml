---
- name: Wezterm
  tags:
    - wezterm
  block:
    - name: Symlink config
      ansible.builtin.file:
        src: "{{ role_path }}/files"
        dest: "{{ ansible_user_dir }}/.config/wezterm"
        state: link
