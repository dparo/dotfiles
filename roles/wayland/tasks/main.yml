---
- name: Wayland
  tags:
    - wayland
  block:
  - name: Install
    ansible.builtin.dnf:
      name:
        - wayland-utils
        - xdg-desktop-portal-wlr
        - wl-clipboard
        - foot
        - cliphist
        - swappy # Wayland native snapshot editing tool, inspired by Snappy on macOS
      state: present
    become: true
